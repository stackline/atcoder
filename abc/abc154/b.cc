#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  string s;
  cin >> s;

  for (int i = 0; i < (int) s.size(); i++) {
    cout << 'x';
  }
  cout << endl;
  return 0;
}
