#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  cin.tie(nullptr); // Do not flush "cout" when processing "cin".

  int a, b;
  cin >> a >> b;

  cout << ((2 * a + 100) - b) << endl;
  return 0;
}
